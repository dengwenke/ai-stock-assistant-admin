# Execution Standards

- 规则、设计、执行标准统一以 `docs/knowledge-base` 为准。
- 菜单与路由保持一致修改（`src/config/menu.ts` 与 `src/router/index.ts`）。
- 统一通过 `src/api/client.ts` 调用后端接口。
- 新增规则文档统一放到 `docs/knowledge-base` 下对应分类目录。
